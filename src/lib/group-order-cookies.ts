/** HttpOnly cookie storing joiner's opaque token after successful join (participant auth). */
export const GROUP_ORDER_JOIN_TOKEN_COOKIE = "mennyu_go_join";

export const GROUP_ORDER_JOIN_COOKIE_MAX_AGE_SEC = 60 * 60 * 24; // 24h, aligned with session TTL
