/** Re-export shared modifier config types for vendor add-to-cart UI. */
export type {
  ModifierConfigForUI,
  ModifierGroupLinkForUI,
  ModifierOptionForUI,
  NestedModifierGroupForUI,
} from "@/lib/modifier-config";
